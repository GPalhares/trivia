const initialState = {
  name: '',
  assertions: '',
  score: '',
  gravatarEmail: '',
};

const player = (state = initialState, action) => {
  switch (action.type) {
  // case '':
  //   return '';

  default:
    return state;
  }
};

export default player;
